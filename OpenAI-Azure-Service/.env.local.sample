# Simula perfil `COMUM`, `DEV` ou `PREVIEW`, apenas em ambiente de desenvolvimento.
# Use o arquivo `.env.local` na raiz do projeto para setar essa variável.
#
# SIGA_PROFILE=PERFIL_COMUM
# SIGA_PROFILE=PERFIL_PREVIEW
# SIGA_PROFILE=PERFIL_DEV
